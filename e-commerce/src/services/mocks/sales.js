const getSales = () => {
  // Simulated sales data
};

const addSale = (sale) => {
  // Simulated function to add a sale
};

const removeSale = (saleId) => {
  // Simulated function to remove a sale
};

const updateSale = (saleId, updatedInfo) => {
  // Simulated function to update a sale
};

export const sales = { getSales, addSale, removeSale, updateSale };
